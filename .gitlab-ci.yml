image: quay.io/fedora-ostree-desktops/buildroot:43

stages:
  - build

build hyprland-dms:
  stage: build
  interruptible: true
  script:
    - buildah build
        --build-arg FEDORA_MAJOR_VERSION=43
        -t hyprland-dms:43
        .
  timeout: 90m
